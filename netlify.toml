[build]
  base = "client"
  command = "npm install && npm run build"
  publish = "dist"
  ignore = "git diff --quiet HEAD^ HEAD ./"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
  CI = "false"
